version: 2
jobs:
  test:
    docker:
      - image: kanayannet/image_limit_resize:latest
    steps:
      #- checkout
      - run: yum install -y sudo
      - run: sudo bundle install
      - run: bundle exec ruby test/image_limit_resize_test.rb
workflows:
  version: 2
  test:
    jobs:
      - test
